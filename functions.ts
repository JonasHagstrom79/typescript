/**
 * Squeares a number
 * @param num number to be squared
 * @returns 
 */
function square(num: number){
    return num * num; 
}

function greet(person: string = "Default value") {
    return "Hello " + person;
}
